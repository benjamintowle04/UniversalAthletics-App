spring.application.name=ua-backend

# Application database and server configuration
# This file configures the MySQL database connection, Hibernate settings, and server port

# Database Configuration
## Database configuration - prefer environment variables for production (Heroku/JawsDB)
# Example: Heroku/JawsDB provides a JAWSDB_URL like mysql://user:pass@host:3306/dbname
# The DataSourceConfig will parse JAWSDB_URL automatically. Otherwise Spring will
# use SPRING_DATASOURCE_URL/USERNAME/PASSWORD if provided here or fall back to localhost.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://h40lg7qyub2umdvb.cbetxkdyhwsb.us-east-1.rds.amazonaws.com:3306/mki8s7vu2lrwb2ve}
spring.datasource.username=lxtwg20ygse7htlr
spring.datasource.password=e5gw6qgvfdut6jvv
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# Flyway migrations
# Make Flyway opt-in so local development doesn't fail if the remote DB differs.
# Set the environment variable FLYWAY_ENABLED=true to enable migrations (e.g. on Heroku).
# Disable Flyway on Heroku by default to avoid unsupported DB detection issues.
# To enable migrations, set FLYWAY_ENABLED=true in the environment or change this to true.
# Make Flyway opt-in by default; Heroku setup enables FLYWAY_ENABLED=true.
spring.flyway.enabled=${FLYWAY_ENABLED:false}
# Location where Flyway looks for migrations (classpath:db/migration is default)
# Flyway locations
spring.flyway.locations=classpath:db/migration
# Debug logging for Flyway + auto-config (temporary) to help diagnose deploy-time behavior
logging.level.org.flywaydb=DEBUG
logging.level.org.springframework.boot.autoconfigure.flyway=DEBUG
logging.level.org.springframework.boot.autoconfigure=DEBUG
# If you are applying Flyway to an existing schema, baseline on migrate avoids failures
spring.flyway.baseline-on-migrate=true




# Specify Java version
java.runtime.version=21

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Google Cloud Storage Configuration
gcp.bucket.name=universal-athletics-info
gcp.project.id=Universal-Athletics

gcp.credentials.path=${GCP_CREDENTIALS_PATH:}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Jackson Configuration
# This makes Jackson more forgiving when serializing empty Java beans
spring.jackson.default-property-inclusion=non-null

#Logging Configuration
logging.level.root=INFO
logging.level.com.universalathletics=DEBUG

#CORS configuration
spring.web.cors.allowed-origins=http://localhost:8081,http://127.0.0.1:8081,http://192.168.0.49:8080
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true



# Server Configuration
server.port=${PORT:8080}

# Optional CORS: allow override via env var (comma separated)
spring.web.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:8081,http://127.0.0.1:8081,http://192.168.0.49:8080}
